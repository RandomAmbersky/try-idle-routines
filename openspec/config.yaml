schema: spec-driven

# Project context (optional)
# This is shown to AI when creating artifacts.
# Подробности: openspec/project.md
context: |
  Проект на Rust: idle RTS (стратегия в реальном времени с idle/incremental-прогрессом).
  Спека и изменения — OpenSpec spec-driven; команды Cursor: /opsx-propose, /opsx:apply, /opsx-archive, /opsx-explore.

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
