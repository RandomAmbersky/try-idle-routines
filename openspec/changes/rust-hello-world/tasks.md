## 1. Cargo-проект в корне

- [ ] 1.1 Создать `Cargo.toml`: один binary-крейт, имя пакета согласовано с репозиторием (например `try-idle-routines`), без внешних зависимостей для hello world
- [ ] 1.2 Добавить `src/main.rs` с `fn main`, печатающим в stdout строку, содержащую `Hello, world!`
- [ ] 1.3 Выполнить `cargo build` в корне; при появлении `Cargo.lock` зафиксировать его в git

## 2. Проверка и документация репо

- [ ] 2.1 Убедиться, что `cargo run` из корня завершается с кодом 0 и stdout содержит `Hello, world!`
- [ ] 2.2 Обновить `openspec/project.md`: кратко указать команды `cargo build` / `cargo run` для основного бинаря (по шаблону раздела «Что дописать по мере развития кода»)
