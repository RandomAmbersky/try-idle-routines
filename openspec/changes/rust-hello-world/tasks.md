## 1. Cargo scaffold

- [x] 1.1 Add root `Cargo.toml` with `package.name = "try_idle_routines"` and `edition = "2021"`.
- [x] 1.2 Add `src/main.rs` that prints `Hello, world!` to stdout.

## 2. Repo hygiene and verification

- [x] 2.1 Ensure `.gitignore` includes `target/` (and common Rust patterns if absent).
- [x] 2.2 Run `cargo check` and `cargo run` from the repository root; confirm output is `Hello, world!`.
- [x] 2.3 Update `CLAUDE.md` **Build & Test** section with Rust commands (`cargo check`, `cargo run`).
